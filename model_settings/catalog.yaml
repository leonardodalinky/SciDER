# Unified Model Catalog
#
# Single source of truth for all models SciDER knows about, regardless of
# provider. Each entry declares its provider, LiteLLM model id, what it can do
# (capabilities), which env vars are required, and any default LiteLLM kwargs.
#
# ``supports_vision: true`` means the model accepts image inputs. When a role's
# registered model has vision, the corresponding agent's system prompt includes
# extra guidance about using Read on images and verifying matplotlib plots.
#
# Add a new model: drop a new entry here, then optionally point a role at it
# via model_settings/role_defaults.yaml or via the frontend Settings page.

models:
  # ---- Gemini (completion) ----
  gemini-2.5-flash-lite:
    provider: gemini
    litellm_id: gemini/gemini-2.5-flash-lite
    capabilities: [completion]
    supports_vision: true
    requires_env: [GEMINI_API_KEY]
    default_params:
      reasoning_effort: low

  gemini-2.5-flash:
    provider: gemini
    litellm_id: gemini/gemini-2.5-flash
    capabilities: [completion]
    supports_vision: true
    requires_env: [GEMINI_API_KEY]
    default_params:
      reasoning_effort: low

  gemini-2.5-pro:
    provider: gemini
    litellm_id: gemini/gemini-2.5-pro
    capabilities: [completion]
    supports_vision: true
    requires_env: [GEMINI_API_KEY]
    default_params:
      reasoning_effort: low

  gemini-3.1-flash-lite-preview:
    provider: gemini
    litellm_id: gemini/gemini-3.1-flash-lite-preview
    capabilities: [completion]
    supports_vision: true
    requires_env: [GEMINI_API_KEY]
    default_params:
      reasoning_effort: low

  gemini-3-flash-preview:
    provider: gemini
    litellm_id: gemini/gemini-3-flash-preview
    capabilities: [completion]
    supports_vision: true
    requires_env: [GEMINI_API_KEY]
    default_params:
      reasoning_effort: low

  gemini-3.1-pro-preview:
    provider: gemini
    litellm_id: gemini/gemini-3.1-pro-preview
    capabilities: [completion]
    supports_vision: true
    requires_env: [GEMINI_API_KEY]
    default_params:
      reasoning_effort: low

  gemini-3.5-flash:
    provider: gemini
    litellm_id: gemini/gemini-3.5-flash
    capabilities: [completion]
    supports_vision: true
    requires_env: [GEMINI_API_KEY]
    default_params:
      reasoning_effort: low

  # ---- OpenAI (completion) ----
  gpt-5-nano:
    provider: openai
    litellm_id: gpt-5-nano
    capabilities: [completion]
    supports_vision: true
    requires_env: [OPENAI_API_KEY]
    default_params:
      reasoning:
        effort: minimal

  gpt-5-mini:
    provider: openai
    litellm_id: gpt-5-mini
    capabilities: [completion]
    supports_vision: true
    requires_env: [OPENAI_API_KEY]
    default_params:
      reasoning:
        effort: low

  gpt-5:
    provider: openai
    litellm_id: gpt-5
    capabilities: [completion]
    supports_vision: true
    requires_env: [OPENAI_API_KEY]
    default_params:
      reasoning:
        effort: low

  gpt-5-pro:
    provider: openai
    litellm_id: gpt-5-pro
    capabilities: [completion]
    supports_vision: true
    requires_env: [OPENAI_API_KEY]
    default_params:
      reasoning:
        effort: low

  gpt-5-chat:
    provider: openai
    litellm_id: gpt-5-chat
    capabilities: [completion]
    supports_vision: true
    requires_env: [OPENAI_API_KEY]

  gpt-5.4-mini:
    provider: openai
    litellm_id: gpt-5.4-mini
    capabilities: [completion]
    supports_vision: true
    requires_env: [OPENAI_API_KEY]
    default_params:
      reasoning:
        effort: low

  gpt-5.4:
    provider: openai
    litellm_id: gpt-5.4
    capabilities: [completion]
    supports_vision: true
    requires_env: [OPENAI_API_KEY]
    default_params:
      reasoning:
        effort: low

  gpt-5.5:
    provider: openai
    litellm_id: gpt-5.5
    capabilities: [completion]
    supports_vision: true
    requires_env: [OPENAI_API_KEY]
    default_params:
      reasoning:
        effort: low

  o1-mini:
    provider: openai
    litellm_id: o1-mini
    capabilities: [completion]
    supports_vision: false  # text-only; vision never shipped for o1-mini
    requires_env: [OPENAI_API_KEY]

  o3-mini:
    provider: openai
    litellm_id: o3-mini
    capabilities: [completion]
    supports_vision: false  # o3-mini is text-only per OpenAI docs
    requires_env: [OPENAI_API_KEY]

  o3:
    provider: openai
    litellm_id: o3
    capabilities: [completion]
    supports_vision: true
    requires_env: [OPENAI_API_KEY]

  o4-mini:
    provider: openai
    litellm_id: o4-mini
    capabilities: [completion]
    supports_vision: true
    requires_env: [OPENAI_API_KEY]

  # ---- Anthropic (completion) ----
  # Used by experiment_coding role when CODING_AGENT_VERSION=claude_sdk, but
  # also callable directly via litellm if you'd rather mix Claude in elsewhere.
  # All current Claude 4.x models accept image inputs.
  claude-haiku-4-5:
    provider: anthropic
    litellm_id: anthropic/claude-haiku-4-5
    capabilities: [completion]
    supports_vision: true
    requires_env: [ANTHROPIC_API_KEY]
    default_params:
      reasoning_effort: low

  claude-sonnet-4-5:
    provider: anthropic
    litellm_id: anthropic/claude-sonnet-4-5
    capabilities: [completion]
    supports_vision: true
    requires_env: [ANTHROPIC_API_KEY]
    default_params:
      reasoning_effort: low

  claude-sonnet-4-6:
    provider: anthropic
    litellm_id: anthropic/claude-sonnet-4-6
    capabilities: [completion]
    supports_vision: true
    requires_env: [ANTHROPIC_API_KEY]
    default_params:
      reasoning_effort: low

  claude-opus-4-5:
    provider: anthropic
    litellm_id: anthropic/claude-opus-4-5
    capabilities: [completion]
    supports_vision: true
    requires_env: [ANTHROPIC_API_KEY]
    default_params:
      reasoning_effort: low

  claude-opus-4-6:
    provider: anthropic
    litellm_id: anthropic/claude-opus-4-6
    capabilities: [completion]
    supports_vision: true
    requires_env: [ANTHROPIC_API_KEY]
    default_params:
      reasoning_effort: low

  # ---- Embedding ----
  text-embedding-3-small:
    provider: openai
    litellm_id: text-embedding-3-small
    capabilities: [embedding]
    requires_env: [OPENAI_API_KEY]

  text-embedding-3-large:
    provider: openai
    litellm_id: text-embedding-3-large
    capabilities: [embedding]
    requires_env: [OPENAI_API_KEY]

  gemini-embedding-001:
    provider: gemini
    litellm_id: gemini/gemini-embedding-001
    capabilities: [embedding]
    requires_env: [GEMINI_API_KEY]

  # ---- Self-hosted (vLLM, OpenAI-compatible) ----
  openscider-qwen3.6-27b:
    provider: openai                     # litellm uses OpenAI route for vllm
    litellm_id: openai/openscider-qwen3.6-27b   # name after "openai/" must match --served-model-name
    capabilities: [completion]
    supports_vision: false               # we trained text-only; flag VL only if you serve the VL adapter
    requires_env: []
    default_params:
      api_base: http://openscider-qwen36-27b-srv:8000/v1
      api_key: qwgroup
